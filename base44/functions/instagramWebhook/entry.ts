import { createClientFromRequest } from 'npm:@base44/sdk@0.8.44';
import { constantTimeEqual,enforceExistingUserSecurity,securityErrorResponse } from '../../shared/functionSecurity.js';
const clean=(value,max=1000)=>String(value||'').trim().slice(0,max);
async function hmac(secret,body){const key=await crypto.subtle.importKey('raw',new TextEncoder().encode(secret),{name:'HMAC',hash:'SHA-256'},false,['sign']),sig=await crypto.subtle.sign('HMAC',key,new TextEncoder().encode(body));return[...new Uint8Array(sig)].map(b=>b.toString(16).padStart(2,'0')).join('');}
export default async function(req){const requestId=crypto.randomUUID();try{const url=new URL(req.url);if(req.method==='GET'){const expected=Deno.env.get('INSTAGRAM_VERIFY_TOKEN')||'',token=url.searchParams.get('hub.verify_token')||'',challenge=url.searchParams.get('hub.challenge')||'';if(url.searchParams.get('hub.mode')==='subscribe'&&expected&&token&&constantTimeEqual(token,expected))return new Response(challenge,{status:200,headers:{'Content-Type':'text/plain'}});return new Response('forbidden',{status:403});}if(req.method!=='POST')return new Response('method_not_allowed',{status:405});const contentLength=Number(req.headers.get('content-length')||0);if(contentLength>1024*1024)return new Response('payload_too_large',{status:413});const rawBody=await req.text();if(rawBody.length>1024*1024)return new Response('payload_too_large',{status:413});const base44=createClientFromRequest(req),appSecret=Deno.env.get('INSTAGRAM_APP_SECRET')||'',signature=req.headers.get('x-hub-signature-256')||'';if(!appSecret)return Response.json({error:'instagram_webhook_not_configured',request_id:requestId},{status:503});if(signature){const expected=`sha256=${await hmac(appSecret,rawBody)}`;if(!constantTimeEqual(signature,expected))return new Response('invalid_signature',{status:401});}else{const tester=await base44.auth.me().catch(()=>null);if(!tester)return new Response('invalid_signature',{status:401});await enforceExistingUserSecurity(base44,req,tester,{permission:'integrations.manage',requireMfa:true,source:'instagram_webhook_test'});}const legalEntityId=clean(Deno.env.get('INSTAGRAM_LEGAL_ENTITY_ID'),100),unitId=clean(Deno.env.get('INSTAGRAM_UNIT_ID'),100);if(!legalEntityId||!unitId)return Response.json({error:'instagram_scope_not_configured',request_id:requestId},{status:503});const unit=await base44.asServiceRole.entities.Unit.get(unitId).catch(()=>null);if(!unit||unit.legal_entity_id!==legalEntityId)return Response.json({error:'instagram_scope_invalid',request_id:requestId},{status:503});const payload=JSON.parse(rawBody||'{}'),accountId=clean(Deno.env.get('INSTAGRAM_ACCOUNT_ID'),200),accessToken=Deno.env.get('INSTAGRAM_ACCESS_TOKEN')||'',events=[];for(const entry of payload.entry||[]){for(const ev of entry.messaging||[])events.push(ev);for(const change of entry.changes||[])if(change.field==='messages'&&change.value)events.push(change.value);}if(payload.field==='messages'&&payload.value)events.push(payload.value);let processed=0;for(const ev of events.slice(0,100)){const senderId=clean(ev.sender?.id,200),recipientId=clean(ev.recipient?.id,200);if(!senderId||ev.message?.is_echo||(accountId&&senderId===accountId))continue;const messageId=clean(ev.message?.mid,240),text=clean(ev.message?.text,4000),attachment=(ev.message?.attachments||[])[0],mediaUrl=clean(attachment?.payload?.url,2000);let type='TEXT';if(attachment)type=attachment.type==='image'?'IMAGE':attachment.type==='audio'?'AUDIO':'DOC';if(!text&&!mediaUrl)continue;if(messageId){const duplicate=(await base44.asServiceRole.entities.Message.filter({provider_message_id:messageId},'-created_date',2).catch(()=>[]))[0];if(duplicate)continue;}const conversations=await base44.asServiceRole.entities.Conversation.filter({channel:'INSTAGRAM',legal_entity_id:legalEntityId,unit_id:unitId},'-last_message_at',500).catch(()=>[]);let conversation=conversations.find(c=>clean(c.metadata?.instagram_user_id,200)===senderId),customer=conversation?await base44.asServiceRole.entities.Customer.get(conversation.customer_id).catch(()=>null):null;if(!customer){let username='';if(accessToken){try{const response=await fetch(`https://graph.instagram.com/v21.0/${encodeURIComponent(senderId)}?fields=username,name`,{headers:{Authorization:`Bearer ${accessToken}`},signal:AbortSignal.timeout(4000)});if(response.ok){const info=await response.json();username=clean(info.username||info.name,160);}}catch{console.warn(`[instagramWebhook:${requestId}] profile_lookup_failed`);}}customer=await base44.asServiceRole.entities.Customer.create({full_name:username?`@${username}`:'Cliente Instagram',phones:[],status:'active',last_inbound_at:new Date().toISOString(),unit_id:unitId,legal_entity_id:legalEntityId,preferred_unit_name:unit.name,notes:'Contato recebido pelo canal Instagram.',metadata:{source:'instagram'}});}else await base44.asServiceRole.entities.Customer.update(customer.id,{last_inbound_at:new Date().toISOString()});if(!conversation)conversation=await base44.asServiceRole.entities.Conversation.create({customer_id:customer.id,legal_entity_id:legalEntityId,unit_id:unitId,channel:'INSTAGRAM',status:'OPEN',handoff_required:true,service_mode:'human',assigned_queue:'social',last_message_at:new Date().toISOString(),metadata:{source:'instagram',instagram_user_id:senderId,instagram_account_id:recipientId}});const message=await base44.asServiceRole.entities.Message.create({conversation_id:conversation.id,legal_entity_id:legalEntityId,unit_id:unitId,direction:'IN',type,text,media_file_id:mediaUrl||undefined,provider_message_id:messageId||undefined,sender_type:'customer',delivery_status:'received',received_at:new Date().toISOString(),raw_payload:{source:'instagram',instagram_message_id:messageId,has_attachment:Boolean(mediaUrl)},ai_pending:false,ai_answered:false});await base44.asServiceRole.entities.Conversation.update(conversation.id,{status:'OPEN',handoff_required:true,service_mode:'human',last_message_id:message.id,last_message_at:new Date().toISOString()});processed++;}return Response.json({status:'ok',processed,request_id:requestId});}catch(error){if(error?.name==='SecurityError')return securityErrorResponse(error);console.error(`[instagramWebhook:${requestId}]`,error?.name||'failed');return Response.json({status:'error',request_id:requestId});}}
